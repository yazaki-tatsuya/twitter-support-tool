# TwitterSupportTool

Legacy Java Web application assets with Git-managed database definition assets.

## DB assets

Oracle DB definition assets are placed under `db/`.
Actual data dump files such as `backup.dmp` are intentionally excluded from Git.
