# schema

スキーマ単位で見たい場合の参照用DDLです。

- `TENNISDBUSR_schema.sql`
- `TWITTERTOOLUSR_schema.sql`
- `RBWEGUSR_schema.sql`
- `CELONISUSR_schema.sql`

実行順序を管理したい場合は、`db/ddl/` 配下のオブジェクト種別ごとのDDLと `run_all_ddl.sql` を使ってください。
